[phases.setup]
nixPkgs = ["ffmpeg", "libsndfile"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn python_server_optimized:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
